// import mySqlConnection from './index';
//
//
// module.export = {
//   // get for username display on nav
//   get(req, res) {
//     mySqlConnection.query('SELECT name, city, zipcode FROM users WHERE id = 350', (err, rows) => {
//       if (err) {
//         throw err;
//       } else {
//         res.send(JSON.stringify(rows));
//       }
//     });
//   },
  // post(req, res) {
  //   mySqlConnection.query(`SELECT id_items FROM cart WHERE user_id = ${id}`, (err, rows) => {
  //     if(err) {
  //       throw err;
  //     } else {
  //
  //     }
  //   })
  // }
// };
